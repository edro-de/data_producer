## to spin up the services
docker compose -f docker-compose.yml up --build

## to stop
docker compose -f docker-compose.yml down -v

## Consuming messages from the terminal (for local dev)
docker exec -it kafka1 kafka-console-consumer --bootstrap-server localhost:9092 --topic transactions --property print.key=true --property key.separator=" | "
